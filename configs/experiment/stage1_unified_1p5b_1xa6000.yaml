defaults:
  - stage1_unified.yaml
  - ../runtime/a6000_1x.yaml

run:
  name: stage1_unified_deepseek_1p5b_1xa6000
  output_dir: ${COT_SAFETY_RUN_ROOT:-runs}/stage1_unified_deepseek_1p5b_1xa6000

probe:
  hidden_extraction:
    batch_size_per_gpu: 40
    train_shards: 1
